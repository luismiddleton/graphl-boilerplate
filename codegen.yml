overwrite: true
schema: "./src/typedefs.ts"
documents: null
config:
  useIndexSignature: true
generates:
  ./src/types/resolvers-types.ts:
    plugins:
      - typescript
      - typescript-resolvers
  ./graphql.schema.json:
    plugins:
      - "introspection"
